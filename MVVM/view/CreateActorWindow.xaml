<Window x:Class="MVVM.CreateActorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVM"
        xmlns:vm="clr-namespace:MVVM.VM"
        
        mc:Ignorable="d"
        Title="Создать актёра" MaxHeight="350" MaxWidth="230" MinHeight="350" MinWidth="230">
    <Window.DataContext>
        <vm:AddActorWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Имя" FontSize="20" Margin="5"/>
            <TextBox Margin="5" />
            <TextBlock Text="Фамилия" FontSize="20" Margin="5"/>
            <TextBox  Margin="5" />
            <TextBlock Text="Отчество" FontSize="20" Margin="5"/>
            <TextBox  Margin="5" />
            <Button Content="Добавление" MinHeight="50" Margin="15"/>
        </StackPanel>
    </Grid>
</Window>
